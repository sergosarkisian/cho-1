if $hostname == $$myhostname and $programname == [
	"apache",
	"apache-cone",
	"haproxy",
	"nginx",
	"nginx-cone",
	"shell",
	"systemd"
	]
then {
	action(type="mmjsonparse")
	set $!use_mod = $!use_mod + "mmjsonparse, ";
	call engine & stop
}



