+Небольшой процент логов не имеет заголовка From.

+Концепция сессионного белого листа

+Отсутствует мониторинг состояния приложения "cone3 mct_spamflt"

+SMTP-сессии не отображаются в monitor.edss.ee (SMTP low-level session headers)

+Визуальный просмотр алиасов (Web PG admin)

+Логиторинг импорт-job'ов

+PAM auth
 
+ Обновление кода на TSC (added: spamassassin-интеграция, SPF, DKIM)

+ auto DKIM-signing if cert is available

+ SPF policy - is my server (current mail server) permittable relay? (this check is not in exim source)

+ add culculated rules
DNSBL +  Dangerous (ext) archived content in attachment(s)

+ add subject to logs, use {escape:} syntax
