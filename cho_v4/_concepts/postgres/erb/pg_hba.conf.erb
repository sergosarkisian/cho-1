# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     trust
# IPv4 local connections:
host    all             all             127.0.0.1/32            trust

# replication connections
<% @replication.each do |address| -%>
host     replication     postgres         <%= address -%>            trust
<% end -%>

# connections
<% @connections.each do |address,parameters| -%>
host     <%= parameters["database"] -%>   <%= parameters["user"] -%>         <%= address -%>            md5
<% end -%>


<%=  -%>

<%=  -%>

 
